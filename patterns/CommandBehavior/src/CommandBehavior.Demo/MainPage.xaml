<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             x:Class="CommandBehavior.Demo.MainPage"
             Title="Order list">

    <ScrollView>
        <VerticalStackLayout
            Padding="30,20"
            Spacing="15">

            <Label
                Text="Quick filter"
                Style="{StaticResource SubHeadline}" />

            <!-- Behaviour-bridged command: Entry has no native Command for TextChanged, so
                 EventToCommandBehavior bridges the event into the same commanding model the
                 Button below uses directly. -->
            <Entry
                Text="{Binding FilterText, Mode=TwoWay}"
                Placeholder="Customer reference">
                <Entry.Behaviors>
                    <toolkit:EventToCommandBehavior
                        EventName="TextChanged"
                        Command="{Binding ApplyFilterCommand}" />
                </Entry.Behaviors>
            </Entry>

            <!-- Direct command binding: Button natively supports Command. -->
            <Button
                Text="Clear"
                Command="{Binding ClearFilterCommand}"
                HorizontalOptions="Fill" />

            <CollectionView ItemsSource="{Binding FilteredOrders}">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Label Text="{Binding CustomerReference}" Padding="0,5" />
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
